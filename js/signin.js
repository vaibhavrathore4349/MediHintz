const loginForm = document.getElementById("loginForm"), loginPassword = document.getElementById("loginPassword"), loginToggle = document.getElementById("loginToggle"), loginMessage = document.getElementById("loginMessage");
loginToggle.addEventListener("click", () => { const concealed = loginPassword.type === "password"; loginPassword.type = concealed ? "text" : "password"; loginToggle.textContent = concealed ? "Hide" : "Show"; });
loginForm.addEventListener("submit", (event) => { event.preventDefault(); if (!loginForm.checkValidity()) { loginMessage.textContent = "Please enter your email and password."; loginMessage.style.color = "#bb4c3e"; return; } loginMessage.textContent = "Welcome back — taking you home."; loginMessage.style.color = "#00866d"; window.setTimeout(() => showScreen("home"), 700); });
